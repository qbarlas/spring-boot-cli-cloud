# Spring boot cli with cloud cli

## Usage
```
docker run --rm qbarlas/spring-boot-cli-cloud spring help
```

```
docker run --rm qbarlas/spring-boot-cli-cloud spring encrypt some-secret -key secret-key
```